import { Navbar } from "../Navbar";
import { Link } from "react-router-dom";

export const Animals = () => {
    return (
        <>
        <Navbar></Navbar>
        <p>Animal-page works</p>
        </>
    );
};